type User {
  id: ID! @unique
}

type Task {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  subject: String!
  important: Boolean! @default(value: "false")
  completed: Boolean! @default(value: "false")
  owner: User!
}