type User {
  id: ID! @unique
  email: String! @unique
  name: String
}

type Task {
  id: ID! @unique
  createdAt: DateTime!
  updatedAt: DateTime!
  subject: String!
  important: Boolean! @default(value: "false")
}